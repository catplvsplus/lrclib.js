{
  "name": "@lrclibjs/monorepo",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "preview": "lerna run preview",
    "dev": "lerna run dev"
  },
  "devDependencies": {
    "@reciple/utils": "^9.3.2",
    "@types/node": "^22.4.0",
    "lerna": "^8.1.8",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "svelte-meta-tags": "^3.1.4"
  }
}
